# Gradle-project properties voor EmuFlow Android Agent
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.configuration-cache=true
org.gradle.parallel=true

# Android-specifieke properties
android.useAndroidX=true
android.enableJetifier=false

# Kotlin-code generatie
kotlin.code.style=official

# Compose-compiler metrics (optioneel, handig voor debugging)
# android.compose.compiler.metricsPath=${project.buildDir}/compose_metrics
